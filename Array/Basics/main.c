#include <stdio.h>

int main()
{

    // array declaration
    int a[2]; // a[0] , a[1]
    printf("%i \n", a[0] = 25);
    printf("%i \n", a[1] = 35);

    // int a[2] = {25, 35};
    // printf("%i \n", a[0]);
    // printf("%i \n", a[1]);

    return 0;
}