#include <stdio.h>

// one dimensional array example 2
int main()
{
    int a[2] = {52, 32}; // a[0] =52 , a[1] = 32
    for (int i = 0; i < 2; i++)
    {
        printf("%i\n", a[i]);
    }

    return 0;
}